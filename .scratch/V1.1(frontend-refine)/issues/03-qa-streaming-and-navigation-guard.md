# 03 — 完善问答流式交互与离开保护

**What to build:** 按参考聊天页重塑问题、回答和输入区的阅读层级，同时保留现有 Markdown 和 SSE 行为。用户问题靠右、回答靠左，输入区位于主区域底部；回答下方仅提供反馈图标入口。生成期间切换会话、进入管理后台、个人中心或其他会卸载问答的导航时，必须先显式停止并在停止成功后离开。

**Blocked by:** 02 — 重塑问答工作台桌面布局与会话导航.

**Status:** ready-for-agent

- [ ] 问答正文按用户问题靠右、回答靠左的结构展示
- [ ] 输入区保留 Enter 发送、Shift+Enter 换行、加载和禁用状态
- [ ] 生成期间显示停止按钮，继续调用现有显式取消接口
- [ ] 正确展示 started、delta、completed、refused、failed、stopped 和 unfinished 的现有用户状态
- [ ] 回答下方反馈图标打开现有反馈对话框，已反馈后不可重复提交
- [ ] 新对话说明答案仅基于所选知识库可用企业文档
- [ ] 不展示硬编码推荐问题或不存在的深度思考模式
- [ ] 生成期间离开触发“停止生成并离开”确认；取消确认留在原页
- [ ] 停止接口失败时不导航并显示错误；停止成功后才导航
- [ ] 通过真实 Java SSE 和反馈接口验证流式及离开路径
