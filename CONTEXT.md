# Sage Vault

企业内部知识库的自然语言问答系统。首发场景聚焦**规章制度问答**:答案必须精确、可追溯到原文条款、近乎零幻觉。基于 RuoYi-Cloud 基座二开,采用 Java 业务中台 + Python AI 服务的异构协作架构。

## Language

**制度文档 (Regulation Document)**:
一篇企业规章制度,是知识的基本单位,也是**权限的基本单位**——可见范围挂在文档上,而非段落上。
_Avoid_: 资料、文件、知识条目

**可见范围 (Visibility Scope)**:
一篇制度文档允许被哪些角色/部门看到的集合,以**标签**形式落到该文档的每个 chunk 上(公开制度打 `PUBLIC`)。检索是**权限感知**的:用户无权看到的文档,其内容不得被召回,即使只是用于喂给大模型生成答案也不行。复用 RuoYi 的角色/部门体系作为权威源。第一版做**文档级**(不做段落级)。
_Avoid_: 密级、权限组

**授权声明 (Authorization Claims)**:
Java 中台随问答请求下发给 Python 的、代表当前用户身份的角色/部门标签集合。Python 按它与 chunk 标签求交来执行过滤。授权在 Java,执行在 Python。见 [ADR-0001](docs/adr/0001-rag-orchestration-and-permission-boundary.md)。
_Avoid_: 权限令牌、token

**例外覆盖层 (Exception Overlay)**:
叠加在标签求交之上的一小段显式 grant/deny 的 documentId 列表,只装"临时单独授权某人看某篇"这类例外,天然短。日常权限靠标签,例外靠它。
_Avoid_: 白名单、黑名单

**知识问答 (Knowledge QA)**:
系统的核心职责——接收自然语言提问,经检索+重排+生成,返回带原文引用的答案。第一版以此为唯一核心功能。
_Avoid_: 智能客服、对话机器人

**引用 (Citation)**:
答案里指向原文出处的溯源锚点,做到**条款级**——定位到"《考勤管理制度》第 3 章第 12 条"并可跳转原文。因此 chunk 必须携带其所属的章/条标识。
_Avoid_: 出处、来源、参考

**拒答 (Refusal)**:
当检索不到足够可信的制度依据时系统的**硬约束**行为:明确说明"未在现行制度中找到相关依据",不猜、不编、不用大模型通用知识兜底。宁可拒答,不可乱答。
_Avoid_: 兜底、降级回答

**片段 (Chunk)**:
检索的基本单位,按制度文档的"条"为界做**结构感知切分**,携带 `文档名/章/节/条号` 元数据与可见范围标签。结构解析失败时降级为定长切块 + 页级引用,并标记"待人工规整"。
_Avoid_: 段落、分片、切片
